# Hassad -- what the hosted app needs (Streamlit Community Cloud reads this file)
# FIRST Global Challenge 2026, Team Lebanon
shapely>=2.0
networkx>=3.0
numpy>=1.26
matplotlib>=3.8
pandas>=2.1
streamlit==1.63.0
folium==0.20.0
streamlit-folium==0.27.4
# The satellite / fire-history evidence modules need more; see requirements-evidence.txt

# 6 Sep 2026: touched so Streamlit Community Cloud reboots the app (it does not reload imported
# modules on a plain file change; a dependency-file change forces a fresh process).

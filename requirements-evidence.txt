# Extra packages for the evidence scripts (satellite.py, firms.py) -- NOT needed to host the app.
#   pip install -r requirements.txt -r requirements-evidence.txt
rasterio>=1.3
pyproj>=3.6
requests>=2.31
pillow>=10
